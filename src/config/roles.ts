// Membership role names in the vACC SSO (auth.vatsim.fr). Shared between the
// browser app and the serverless functions — keep in sync with the roles the
// Digital Team creates in Keycloak.
export const ROLE_ADMIN = 'membership-admin'
export const ROLE_REFERENT = 'membership-referent'
